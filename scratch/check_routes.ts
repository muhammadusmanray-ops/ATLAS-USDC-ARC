import express from 'express';
// @ts-ignore
import { startServer } from './server.ts';

// This is just a scratch script to verify routes
// But server.ts doesn't export startServer.

console.log("Checking server.ts content directly...");
